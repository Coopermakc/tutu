<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @railway_station.title %>
</p>

<% if @railway_station.routs.empty? %>
  <p> Cтанция не входит ни в один маршрут! </p>
<% else %>
  <p>Станция входит в следующие маршруты:</p>
   <ul>
     <% @railway_station.routs.each do |rout| %>
      <li><%= rout.title %> </li>
     <% end %>
   </ul>>
<% end %>

<%= link_to 'Edit', edit_railway_station_path(@railway_station) %> |
<%= link_to 'Back', railway_stations_path %>
