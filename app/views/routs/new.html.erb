<h1>Добавить маршрут</h1>

<%= form_for(@rout) do |form| %>
  <% if @rout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rout.errors.count, "error") %> prohibited this railway_station from being saved:</h2>

      <ul>
      <% @rout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title%>
  </div>

  <div class="actions">
    <%= form.submit 'Создать'%>
  </div>
<% end %>

<%= link_to 'Назад', routs_path %>